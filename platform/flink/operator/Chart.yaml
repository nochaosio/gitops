apiVersion: v2
name: flink-operator
description: Vendored umbrella chart for flink-kubernetes-operator
type: application
version: 0.1.0
# Pin the upstream chart version. Vendor it with: make vendor
# Verify available versions: helm search repo <repo>/flink-kubernetes-operator --versions
dependencies:
  - name: flink-kubernetes-operator
    version: "1.15.0"
    repository: "https://downloads.apache.org/flink/flink-kubernetes-operator-1.15.0/"
