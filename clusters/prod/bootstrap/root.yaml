# App-of-apps raiz do cluster prod. Este é o ÚNICO manifesto aplicado à mão:
#   kubectl apply -f clusters/prod/bootstrap/root.yaml
# Ele sincroniza tudo em clusters/prod/bootstrap/apps (self-manage + plataforma).
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: root
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/nochaosio/gitops.git
    targetRevision: HEAD
    path: clusters/prod/bootstrap/apps
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
